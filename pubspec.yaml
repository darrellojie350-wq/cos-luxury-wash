name: cos_luxury_wash
description: "CO's Luxury Wash — premium on-demand laundry & dry cleaning"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  go_router: ^14.6.0
  intl: ^0.19.0
  shimmer: ^3.0.0
  google_fonts: ^6.2.0
  flutter_animate: ^4.5.0
  iconsax: ^0.0.8
  cached_network_image: ^3.4.1
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
